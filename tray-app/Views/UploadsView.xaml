<UserControl x:Class="MediaPipelineTray.Views.UploadsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BgBrush}">

    <DockPanel>

        <Border DockPanel.Dock="Top"
                Background="{DynamicResource Surface2Brush}"
                BorderBrush="{DynamicResource LineBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Status}" Style="{StaticResource Body}" />
                    <TextBlock Text="{Binding SyncFolder}" Style="{StaticResource Mono}" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Choose a file" Click="OnChooseFile" />
                    <Button Content="Upload largest" Click="OnUploadLargest" />
                    <Button Content="Cancel" Click="OnCancel"
                            Visibility="{Binding IsBusy, Converter={StaticResource TrueToVisible}}" />
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <TextBlock Style="{StaticResource Hint}" Margin="20,18,20,0" MaxWidth="640"
                           HorizontalAlignment="Left"
                           Text="Large files are split into chunks, sent one at a time, and reassembled on the remote. A chunk that fails is retried on its own rather than failing the whole transfer, and cancelling keeps the local parts so the next attempt resumes where this one stopped." />

                <ItemsControl ItemsSource="{Binding Uploads}" Margin="20,16,20,20">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{DynamicResource LineBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Background="{DynamicResource SurfaceBrush}"
                                    Padding="14"
                                    Margin="0,0,0,12">
                                <StackPanel>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding FileName}"
                                                   FontFamily="Cascadia Mono, Consolas"
                                                   FontSize="12.5"
                                                   Foreground="{DynamicResource InkBrush}"
                                                   TextTrimming="CharacterEllipsis" />

                                        <TextBlock Grid.Column="1" Text="{Binding Phase}"
                                                   Style="{StaticResource Caption}"
                                                   FontWeight="Normal" />
                                    </Grid>

                                    <ProgressBar Height="4" Margin="0,9"
                                                 Minimum="0" Maximum="1"
                                                 Value="{Binding Fraction, Mode=OneWay}"
                                                 Style="{StaticResource FlatBar}" />

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding Counts}" Style="{StaticResource Mono}" />
                                        <TextBlock Grid.Column="1" Text="{Binding Detail}"
                                                   Style="{StaticResource Mono}"
                                                   Margin="16,0,0,0"
                                                   TextTrimming="CharacterEllipsis" />
                                    </Grid>

                                    <!-- One square per chunk. Green is sent, ink is in flight,
                                         red failed, and the rest are still to go. -->
                                    <ItemsControl ItemsSource="{Binding Chunks}" Margin="0,10,0,0">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="13" Height="13" Margin="0,0,3,3"
                                                        CornerRadius="2"
                                                        Background="{DynamicResource TrackBrush}"
                                                        ToolTip="{Binding Index}">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSent}" Value="True">
                                                                    <Setter Property="Background"
                                                                            Value="{DynamicResource OkBrush}" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsSending}" Value="True">
                                                                    <Setter Property="Background"
                                                                            Value="{DynamicResource InkBrush}" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsFailed}" Value="True">
                                                                    <Setter Property="Background"
                                                                            Value="{DynamicResource FailBrush}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="No uploads yet."
                           Style="{StaticResource Mono}"
                           Margin="20,0,20,20"
                           Visibility="{Binding Uploads.Count, Converter={StaticResource ZeroToVisible}}" />

            </StackPanel>
        </ScrollViewer>

    </DockPanel>
</UserControl>
